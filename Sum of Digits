number = input("Enter a positive integer: ")

# Initialize sum
sum_of_digits = 0

for digit in number:
    sum_of_digits += int(digit)  # Convert each character to an integer and add it to the sum

print(f"The sum of the digits in {number} is: {sum_of_digits}")
